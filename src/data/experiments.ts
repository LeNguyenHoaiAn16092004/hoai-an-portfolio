/**
 * experiments.ts — Lab experiments
 *
 * Only experiments that can be honestly described belong here.
 * The list is intentionally empty until there is something real to show —
 * the Lab page renders an honest empty state instead of invented work.
 */

import type { Experiment } from './types';

export const experiments: Experiment[] = [];
